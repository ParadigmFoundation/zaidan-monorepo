---
kind: pipeline
type: docker
name: default

steps:
- name: test
  image: golang:1.13
  volumes:
  - name: gomod
    path: /go/pkg/mod
  commands:
    - make test

volumes:
  - name: gomod
    host: 
      path: /tmp/drone/zaidan/gomod
